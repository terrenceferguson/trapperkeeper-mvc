using Microsoft.AspNetCore.Mvc;

namespace trapperkeeper_mvc.Controllers
{
    public class BaseController : Controller
    {
        // This might not be needed at the moment.
        // Just messing around. -TF
    }
}